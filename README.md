# Forking Virtual Machine

A simple byte stream based virtual machine with a few extras.

Goals:

* Allow machines to be built on top of machines (forking)
* Allow saving and resuming execution for remote calls